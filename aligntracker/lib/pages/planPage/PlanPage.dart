import 'package:flutter/material.dart';

class Planpage extends StatelessWidget {
  const Planpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Todays Plan"),
      ),
    );
  }
}
